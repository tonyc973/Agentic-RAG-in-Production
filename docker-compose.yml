version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: rag-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - rag-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  audio_backend:
    build: .
    container_name: rag-audio-backend
    restart: unless-stopped
    command: python audio_local_server.py 
    ports:
      - "5001:5001"
    volumes:
      - .:/app # Syncs your code live
      - whisper_models:/root/.cache/whisper # Persists the AI model
    networks:
      - rag-network

  streamlit-app:
    build: .
    container_name: rag-streamlit
    restart: always
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PINECONE_API_KEY=${PINECONE_API_KEY}
      - SERPER_API_KEY=${SERPER_API_KEY}
      - BROWSERLESS_API_KEY=${BROWSERLESS_API_KEY}
      - DB_CONNECTION=${DB_CONNECTION}
      - WHISPER_URL=${WHISPER_URL}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rag-network

volumes:
  pgdata:
  whisper_models:

networks:
  rag-network:
    driver: bridge